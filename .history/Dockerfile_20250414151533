# 使用 Node.js 作为基础镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制项目文件
COPY . .

# 安装依赖并构建应用
RUN npm install && npm run build

# 暴露端口
EXPOSE 4173

# 启动应用
CMD ["npm", "run", "preview", "--", "--host", "0.0.0.0"] 